# Bybit Trading Bot v3.0 - Windows оптимизированные зависимости
# Использует предкомпилированные wheel пакеты для избежания проблем компиляции

# ═══════════════════════════════════════════════════════════════
# ОСНОВНЫЕ КОМПОНЕНТЫ
# ═══════════════════════════════════════════════════════════════

# FastAPI веб-фреймворк
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Bybit API клиент
pybit==5.7.0

# Конфигурация и валидация
pydantic==2.5.0
pydantic-settings>=2.1.0
python-dotenv==1.0.0

# ═══════════════════════════════════════════════════════════════
# ДАННЫЕ И АНАЛИЗ (предкомпилированные)
# ═══════════════════════════════════════════════════════════════

# Используем более новую версию pandas, которая включает совместимые wheel пакеты
pandas>=2.1.0

# HTTP клиент для API запросов
requests>=2.22.0

# WebSocket для real-time данных
websockets==12.0

# ═══════════════════════════════════════════════════════════════
# ПРИМЕЧАНИЯ ДЛЯ УСТАНОВКИ
# ═══════════════════════════════════════════════════════════════

# Для избежания проблем с компиляцией используйте:
# pip install --only-binary=all -r requirements-windows.txt
#
# Или установите по одному:
# pip install --only-binary=pandas pandas
# pip install --only-binary=numpy numpy
#
# Это заставит pip использовать только предкомпилированные wheel файлы 