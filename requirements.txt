# Bybit Trading Bot v3.0 - Оптимизированные зависимости
# Только необходимые пакеты для веб-сервера

# ═══════════════════════════════════════════════════════════════
# ОСНОВНЫЕ КОМПОНЕНТЫ (обязательные)
# ═══════════════════════════════════════════════════════════════

# FastAPI веб-фреймворк
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Bybit API клиент
pybit==5.7.0

# Конфигурация и валидация
pydantic==2.5.0
pydantic-settings>=2.1.0
python-dotenv==1.0.0

# ═══════════════════════════════════════════════════════════════
# ДАННЫЕ И АНАЛИЗ (только базовые)
# ═══════════════════════════════════════════════════════════════

# Pandas и numpy
pandas>=2.2.0
numpy>=1.26.0

# HTTP/WS клиенты
requests>=2.22.0
aiohttp>=3.9.0
websockets==12.0

# ML и анализ данных
scikit-learn>=1.4.0
aiofiles>=23.2.1
pycryptodome>=3.20.0

# ═══════════════════════════════════════════════════════════════
# УДАЛЕННЫЕ ЗАВИСИМОСТИ (не используются в веб-версии)
# ═══════════════════════════════════════════════════════════════

# ❌ numpy==1.26.2 - будет установлен автоматически с pandas
# ❌ asyncio-mqtt - не используется
# ❌ aiohttp - не используется  
# ❌ aiofiles - не используется
# ❌ redis - не используется
# ❌ sqlalchemy - не используется
# ❌ alembic - не используется
# ❌ loguru - используем стандартный logging
# ❌ prometheus-client - не используется
# ❌ pytest - только для разработки
# ❌ pytest-asyncio - только для разработки
# ❌ httpx - только для разработки
# ❌ passlib - не используется
# ❌ python-multipart - не используется
# ❌ python-socketio - не используется
# ❌ marshmallow - не используется
# ❌ celery - не используется
# ❌ websocket-client - заменен на websockets